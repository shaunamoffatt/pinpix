import * as ACTION_TYPES from "../actions/map_action_types";

export const initialState = {
  location: "",
};

export const MapReducer = (state = initialState, action) => {
  switch (action.type) {}
  case ACTION_TYPES.LOCATION_PERMISSION_SUCCESS:
      return {

      };
}