import React from "react";
import { View, Text } from "react-native";

const ProfileLocationScreen = ({ navigation, route }) => {
  return (
    <View>
      <Text>{} ProfileLocationScreen</Text>
    </View>
  );
};

export default ProfileLocationScreen;
