import React from "react";
import { View, Text } from "react-native";

const SelectPinScreen = ({ navigation, route }) => {
  return (
    <View>
      <Text>{} SelectPinScreen</Text>
    </View>
  );
};

export default SelectPinScreen;
